import ViewsMap from './ViewsMap';
import React from 'react';

export default class ResultModelService {
    RenderItems(items, viewsMapping) {
        return items.map(itm => {
            var name = itm.
        });
    }

}
